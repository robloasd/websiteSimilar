name: 🎉 Release Binary - websiteSimilar

on:
  push:
    tags:
      - '*'
  workflow_dispatch:
  
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: "Check out code"
        uses: actions/checkout@v3
        with: 
          fetch-depth: 0

      - name: "Set up Go"
        uses: actions/setup-go@v5
        with: 
          go-version: '1.23'
          
      - name: "Create release on GitHub"
        uses: goreleaser/goreleaser-action@v5
        with: 
          args: "release --clean"
          version: v2.12.7
          workdir: .
        env: 
          GITHUB_TOKEN: "${{ secrets.GO_BUILD_TOKEN }}"
